/**
 *Write a program to input a string.Convert it into upper case.Print the original 
 *string.Now create another string with the vowels present in the original string.
 *Print the newly created string.
 */
    import java.util.Scanner;
    class exa2
    {
        public static void main()
        {
            Scanner sc=new Scanner(System.in);
            String s,vowel="";
            System.out.println("Enter word");
            s=sc.nextInt().toUpperCase();
            System.out.println(s);
            for()
            {
                if(="A");
                {
                    vowel=vowel+
        }
    }